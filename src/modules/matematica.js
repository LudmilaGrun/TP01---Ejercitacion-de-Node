const PI = 3.14;

const numerosString = ["dos", "cuatro", "ocho", "diez"];

function sumar(a, b) {
    return a + b;
}

function restar(a, b) {
    return a - b;
}

const multiplicar = (a, b) => a * b;

const dividir = (a, b) => a / b;


export {PI, numerosString, sumar, restar, multiplicar, dividir};